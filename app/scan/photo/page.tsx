import { PhotoOcrFlow } from "@/components/scan/PhotoOcrFlow";

export const metadata = { title: "Photo de la composition · CosmetWiki" };

export default function PhotoScanPage() {
  return <PhotoOcrFlow />;
}
